#include <bits/stdc++.h>
#include <cstring>
using namespace std;

int main(){
    int arr[5];
    memset(arr,0,sizeof(arr));
    cout<<sizeof(arr)<<endl;
    for(int item: arr){
        cout<<item<<" ";
    }
}